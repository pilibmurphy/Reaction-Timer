export const MAX_ATTEMPTS = 5;
export const MAX_DELAY_MS = 10_000;

export const ScreenMode = Object.freeze({
  BLUE: "blue",
  RED: "red",
  GREEN: "green",
});

export const BlueMessage = Object.freeze({
  START: "start",
  TOO_SOON: "too-soon",
  RESULT: "result",
  SUMMARY: "summary",
});
